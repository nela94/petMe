{
  "name": "pet-me",
  "version": "0.1.0",
  "description": "needed package",
  "proxy": "http://localhost:3001",
  "main": "index.js",
  "repository": "git@github.com:nela94/PetMe.git",
  "homepage": ".",
  "author": "<github username> <<github email address>>",
  "license": "MIT",
  "scripts": {
    "build": "cd pet-me-frontend && npm install && npm run-script build && cd ..",
    "deploy": "cp -a pet-me-frontend/build/. public/",
    "postinstall": "npm run-script build && npm deploy && echo 'Client built!'"
  },
  "engines": {
    "npm": "6.9.0",
    "node": "11.8.0",
    "yarn": "1.15.2"
  }
}
